from aiogram.types import BotCommand

private = [
    BotCommand(command='ostatki', description='Остатки на счетах'),
    BotCommand(command='dohod', description='Доходы'),
    BotCommand(command='rashod', description='Расходы'),
    BotCommand(command='sheta', description='Счета'),
    BotCommand(command='category', description='Категории'),
    BotCommand(command='mesta', description='Места'),
]